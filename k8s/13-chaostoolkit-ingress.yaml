apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: chaostoolkit
  namespace: shopping-demo
spec:
  rules:
    - host: chaostoolkit.workshop.demo
      http:
        paths:
          - path: /
            backend:
              serviceName: chaostoolkit
              servicePort: 22